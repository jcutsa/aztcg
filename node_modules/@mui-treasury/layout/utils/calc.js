"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.plusCalc = exports.subtractCalc = void 0;

var _toValidCssValue = require("./toValidCssValue");

var subtractCalc = function subtractCalc() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  var valid = args.filter(function (v) {
    return !!v;
  });
  if (!valid.length) return "0px";
  return valid.length === 1 ? (0, _toValidCssValue.toValidCssValue)(valid[0]) : "calc(".concat(args.filter(function (item) {
    return !!item;
  }).map(_toValidCssValue.toValidCssValue).join(" - "), ")");
};

exports.subtractCalc = subtractCalc;

var plusCalc = function plusCalc() {
  for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    args[_key2] = arguments[_key2];
  }

  var valid = args.filter(function (v) {
    return !!v;
  });
  if (!valid.length) return "0px";
  return valid.length === 1 ? (0, _toValidCssValue.toValidCssValue)(valid[0]) : "calc(".concat(args.filter(function (item) {
    return !!item;
  }).map(_toValidCssValue.toValidCssValue).join(" + "), ")");
};

exports.plusCalc = plusCalc;